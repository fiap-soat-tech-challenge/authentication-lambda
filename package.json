{
  "name": "authentication-lambda",
  "version": "1.0.0",
  "description": "Função lambda para autenticação em NodeJS usando tokens de acesso JWT.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jonilson Sousa",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
